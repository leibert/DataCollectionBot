from setuptools import setup

setup(name='datacollcetionbot',
      version='0.1',
      url='http://github.com/leibert/datacollectionbot',
      author='leibert',
      packages=['datacollectionbot'],
      zip_safe=False)
